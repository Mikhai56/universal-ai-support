# Universal AI Support

Universal AI Support is a foundation for an AI-powered customer support service.

## Status

The first deployable prototype is now included.

## Run locally

```bash
npm install
npm start
```

Then open `http://localhost:3000` or `http://localhost:3000/health`.

## Deployment

The included `render.yaml` can be used with Render Blueprint.
